using UnityEngine;

using System.Collections;

namespace FakePhysics
{
    [RequireComponent(typeof(Rigidbody2D))]
    public class FakeRigidbody2D : MonoBehaviour {

        // Use this for initialization
        void Start() {

        }

        // Update is called once per frame
        void Update() {

        }
    }
}
